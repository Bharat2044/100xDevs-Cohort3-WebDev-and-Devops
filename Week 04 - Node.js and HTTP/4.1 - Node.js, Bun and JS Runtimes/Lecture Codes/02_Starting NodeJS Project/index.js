/**
 * You can run this code by using the following command
 *   $ node index.js
 *   $ bun index.js
 *   $ npm run start
 *   $ npm run bharat
 */

// function to add two numbers and return the sum
function sum (a, b) {
    return a + b;
}

// calling the sum function and storing the result in a variable ans
const ans = sum(1, 2);

// printing the result to the console
console.log(ans);